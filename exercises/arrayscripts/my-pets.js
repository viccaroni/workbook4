"use strict"

let allPets = ["Scruffy", "Butters", "Marmalade", "Brisket", "Pixel"]

for(let index=0; index<allPets.length; index++){
    let petNames = allPets[index]; //question on what the [index]
    console.log(petNames);
}